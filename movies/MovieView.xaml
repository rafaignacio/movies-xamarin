<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="movies.MovieView">
    <Grid Padding="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Image Source="{Binding Image}" WidthRequest="60" 
               HeightRequest="60" Grid.RowSpan="4"
               VerticalOptions="StartAndExpand"
               HorizontalOptions="StartAndExpand"/>
        
        <Label Text="{Binding Title}" Grid.Column="1" 
               FontAttributes="Bold" FontSize="12"/>
        <Label Text="{Binding ReleaseDate, StringFormat='{0:dd/MM/yyyy}'}" Grid.Column="1"
               Grid.Row="1" FontSize="12"/>
        <Label Text="{Binding Genres}" Grid.Column="1" 
               Grid.Row="2" FontSize="10"/>
        <Label Text="{Binding Overview}" Grid.Column="1" 
               Grid.Row="3" FontSize="10"/>
    </Grid>
</ContentView>
